@using LisansUstuBasvuruSistemi.Models
@using LisansUstuBasvuruSistemi.Utilities.Dtos
@using LisansUstuBasvuruSistemi.Utilities.Enums
@using BiskaUtil
@using LisansUstuBasvuruSistemi.Utilities.Extensions
@model FrYeterlikBasvuruDto
@{
    var item = Model;

    var tabAdi = "";
    var tabIconClass = "";
    var durumAdi = "";
    var durumIconClass = "";
    var durumAciklama = "";
    var durumColor = "";
    var durumDetay = "";
    var durumDetayIconClass = "";
    var aciklamaDetay = "";

    if (!item.IsOnaylandi.HasValue)
    {
        tabAdi = "Yeni Başvuru";
        tabIconClass = "fa fa-list-alt fa-lg";
        durumAdi = "Enstütü Onayı Bekleniyor";
        durumIconClass = "fa fa-clock-o";
        durumColor = "green";
        durumDetay = "Başvuru Enstitü tarafından kabül edilmedi";
        durumDetayIconClass = "fa fa-comments-o";
        aciklamaDetay = "test2";
    }
    else if (!item.IsOnaylandi.Value)
    {
        tabAdi = "Yeni Başvuru";
        tabIconClass = "fa fa-list-alt fa-lg";
        durumAdi = "Enstütü Tarafından İptal Edildi";
        durumIconClass = "fa fa-thumbs-o-down";
        durumColor = "maroon";
        durumDetay = Model.OnayAciklama.Substring(0, Model.OnayAciklama.Length > 45 ? 45 : Model.OnayAciklama.Length) + "...";
        durumDetayIconClass = "fa fa-comments-o";
        aciklamaDetay = Model.OnayAciklama;
    }
    else if (item.IsOnaylandi.Value)
    {
        tabAdi = "Yeni Başvuru";
        tabIconClass = "fa fa-list-alt fa-lg";
        durumAdi = "Enstütü Tarafından Onaylandı";
        durumIconClass = "fa fa-thumbs-o-up";
        durumColor = "green";
        durumDetay = "Başvuru kabulü Enstitü tarafından gerçekleştirildi";
        durumDetayIconClass = "fa fa-comments-o";
        aciklamaDetay = "test2";
    }
}


<table>
    <tbody>
        <tr style="color:@durumColor">
            <td>
                <i class="@tabIconClass" aria-hidden="true"></i> <span style="font-size:9pt;margin:0px;"> @tabAdi</span>  <span style="font-size:7pt;" title="@durumAciklama"><i style="font-size:8pt;" class="@durumIconClass"></i> @durumAdi</span>
            </td>
        </tr>
        <tr>
            <td>

                @if (durumDetay != "")
                {
                    <span style="font-size:7pt;" title="@aciklamaDetay"><i style="font-size:8pt;" class="@durumDetayIconClass"></i> @durumDetay</span>
                }
            </td>
        </tr>
    </tbody>
</table>

